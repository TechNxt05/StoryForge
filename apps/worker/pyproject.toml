[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "storyforge-worker"
version = "0.1.0"
description = "Background Processing Worker for StoryForge AI"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.8.0",
]
