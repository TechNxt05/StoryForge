[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "storyforge-provider-flux"
version = "0.1.0"
description = "FLUX Image Gen Provider Adapter for StoryForge AI"
requires-python = ">=3.12"
dependencies = [
    "storyforge-runtime",
]
