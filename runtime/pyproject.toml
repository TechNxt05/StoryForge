[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "storyforge-runtime"
version = "0.1.0"
description = "StoryForge Core Agentic Runtime Interfaces"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.8.0",
]
